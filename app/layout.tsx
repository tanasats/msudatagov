import type { Metadata } from "next";
import { Geist, Sarabun, Prompt } from "next/font/google";
import "./globals.css";
import { SessionProvider } from "@/context/SessionContext";
import { Toaster } from "react-hot-toast";
import Providers from "./components/Providers";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const sarabun = Sarabun({
  variable: "--font-sarabun",
  weight: ["100", "200", "300", "400", "500", "600", "700", "800"],
  subsets: ["thai"]
});
const prompt = Prompt({
  variable: "--font-prompt",
  weight: ["100", "200", "300", "400", "500", "600", "700", "800"],
  subsets: ["thai"]
});

export const metadata: Metadata = {
  title: process.env.NEXT_PUBLIC_APP_NAME,
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${sarabun.variable} ${prompt.variable} antialiased`}>
        <SessionProvider> {/* Custom session provider */}
          <Providers> {/* Next Auth session provider */}
            {children}
          </Providers>
        </SessionProvider>
        <Toaster toastOptions={{ duration: 2000 }} />
      </body>
    </html>
  );
}
